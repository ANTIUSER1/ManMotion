/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package pns.api.fileimports;

/**
 *
 * @author Movement
 */
public class LimbDataFormation {

    private static int totalLinesInFile = 0;

//    public static List<Segment> generateSimpleList(String s) {
//        ConvertingToSegment cts = new ConvertingToSegment();
//        List<List<Segment>> segmentList = cts.convertNEW(s);
//        totalLinesInFile = segmentList.size();
//        int p = 0;
//        List<Segment> simpleSegmentList = cts.mkSimpleSegmentList(segmentList);
//        List<Segment> res = new ArrayList<>();
//        List<Segment> frList = FileCalculator.calcSegmList(simpleSegmentList, 0, segmentList.get(0).size());
//        for (int k = 0; k < simpleSegmentList.size(); k++) {
//            System.out.println("");
//
//            //List<Segment> frList = FileCalculator.calcSegmList(simpleSegmentList, k, segmentList.get(0).size());
//            //res.addAll(frList);
//        }
//        for (int k = 0; k < res.size(); k++) {
//            int t = k % segmentList.size();
//            if (t == 0) {
//                p++;
//            }
//            System.out.println("p=" + p + "  t=" + t + " k " + +k + "   sg  " + res.get(k).getId() + "     segmentList.get(k).size()   " + segmentList.get(0).size());
//
//        }
////        System.out.println("  segmentList.size() " + segmentList.size());
//        return res;
//    }
//
//    public static List<Limb> generateLimbData(List<Segment> segmentList) {
//        List<Limb> res = new ArrayList<>();
//        int p = 0;
////        for (int k = 0; k < res.size(); k++) {
////            int t = k % segmentList.size();
////            if (t == 0) {
////                p++;
////            }
////            System.out.println("p=" + p + "  t=" + t + " k " + +k + "   sg  " + segmentList.get(k).getId());
////
////        }
//
//        return res;
//    }
}
